# Line Cook Environment Configuration
# Copy this file to .env and customize for your setup

# OpenCode plugin HTTP server settings (optional)
# Used when running the OpenCode plugin as a standalone server
HOST=localhost
PORT=3000
